/// <reference types="cypress" />

describe("User Books a Studio Session Successfully", () => {
  describe("Authentication", () => {
    it("should register a new user", () => {});

    it("should login user", () => {});
  });

  describe("User can see available Photo Studios", () => {
    context("Can search within available photo studio", () => {});
  });

  describe("User can select a Photo Studio", () => {
    it("can see available sessions in photo studio", () => {});

    context(
      "Can specify a date or date range and see what sessions are available (i.e, unbooked sessions)",
      () => {}
    );

    it("can book a session", () => {});
  });
});
